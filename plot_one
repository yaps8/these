#!/bin/bash

make clean
git checkout $1 -- *.tex
timeout 120s make

if [ $? -eq 124 ]; then
  echo "ERR"
else
  d=`./date`
  p=`./pages`
  t=`./todo_sum`
  w=`./words_sum`
  echo "d, p, t, w: "$d", "$p", "$t", "$w
fi
