#!/bin/bash
trap exit SIGINT

rm -f plot.dat
./list_commits | tac > list
for c in `cat list`; do
  if [ -e done/$c ]; then
    echo "already done"
  else
    echo "plotting "$c"..."
    ./plot_one $c > /dev/null
    touch done/$c
  fi
  echo "done"
done
